# گفت‌وگوی چت‌بات حال و احوال بیماران ام‌اس

یک برنامه وب زیبا برای گفت‌وگوی چت‌باتی با بیماران ام‌اس که به صورت تعاملی سوالات را می‌پرسد و پاسخ‌ها را در فایل CSV ذخیره می‌کند.

## ویژگی‌ها

- 🎨 رابط کاربری زیبا و مدرن
- 💬 گفت‌وگوی تعاملی شبیه چت‌بات
- 📊 ذخیره پاسخ‌ها در فایل CSV برای هر کاربر
- 📱 طراحی واکنش‌گرا (Responsive)
- 🌸 رابط کاربری فارسی با پشتیبانی کامل RTL

## نصب و راه‌اندازی

1. نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

2. اجرای برنامه:
```bash
python app.py
```

3. باز کردن مرورگر و رفتن به:
```
http://localhost:5000
```

## ساختار پروژه

```
survey/
├── app.py                 # فایل اصلی Flask
├── requirements.txt        # وابستگی‌های پروژه
├── templates/
│   └── index.html         # رابط کاربری
└── data/                  # پوشه ذخیره فایل‌های CSV (به صورت خودکار ایجاد می‌شود)
```

## نحوه کار

- هر کاربر یک شناسه منحصر به فرد دریافت می‌کند
- پاسخ‌های هر کاربر در فایل CSV جداگانه با نام `{user_id}.csv` ذخیره می‌شود
- فایل‌های CSV شامل: timestamp, name, question, answer, follow_up

## فایل‌های CSV

داده‌ها در پوشه `data/` ذخیره می‌شوند. هر فایل CSV شامل:
- **timestamp**: زمان ثبت پاسخ
- **name**: نام کاربر
- **question**: سوال
- **answer**: پاسخ
- **follow_up**: اطلاعات تکمیلی (در صورت وجود)

